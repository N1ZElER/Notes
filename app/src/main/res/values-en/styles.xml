<resources>
    <style name="AlertDialogFastStyling" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColorPrimary">@color/abc_primary_text_material_light</item>
        <item name="android:windowTitleStyle">@style/AlertDialogFastStyling.Title</item>
        <item name="android:windowBackground">@drawable/routed_background2</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:listPreferredItemPaddingLeft">24dp</item>
        <item name="android:listPreferredItemPaddingRight">24dp</item>
        <item name="buttonBarButtonStyle">@style/AlertDialogFastStyling.Button</item>
        <item name="android:listChoiceIndicatorSingle">@drawable/abc_btn_radio_material</item>
        <item name="android:listChoiceIndicatorMultiple">@drawable/abc_btn_check_material</item>
    </style>

    <style name="AlertDialogFastStyling.Title" parent="RtlOverlay.DialogWindowTitle.AppCompat">
        <item name="android:textAppearance">@style/AlertDialogFastStyling.Title.TextAppearance</item>
    </style>

    <style name="AlertDialogFastStyling.Title.TextAppearance">
        <item name="android:textSize">@dimen/abc_text_size_title_material</item>
        <item name="android:textColor">@color/abc_primary_text_material_light</item>
    </style>

    <style name="AlertDialogFastStyling.Button" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#0C0D0D</item>
        <item name="android:textSize">14sp</item>
    </style>




    <!-- Основной стиль для AlertDialog -->
    <style name="AlertDialogFastStyling2" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Основной цвет текста для заголовка и сообщения -->
        <item name="android:textColorPrimary">@color/abc_primary_text_material_light</item>

        <!-- Стиль заголовка (цвет и размер текста) -->
        <item name="android:windowTitleStyle">@style/AlertDialogFastStyling.Title</item>

        <!-- Фон окна диалога -->
        <item name="android:windowBackground">@drawable/abc_dialog_material_background_light</item>

        <!-- Отступы слева и справа для элементов списка -->
        <item name="android:listPreferredItemPaddingLeft">24dp</item>
        <item name="android:listPreferredItemPaddingRight">24dp</item>

        <!-- Стиль кнопок в диалоге (цвет текста и размер шрифта) -->
        <item name="buttonBarButtonStyle">@style/AlertDialogFastStyling.Button</item>

        <!-- Индикаторы для выбора в списке (одиночные и множественные) -->
        <item name="android:listChoiceIndicatorSingle">@drawable/abc_btn_radio_material</item>
        <item name="android:listChoiceIndicatorMultiple">@drawable/abc_btn_check_material</item>
    </style>

    <!-- Стиль для заголовка диалога -->
    <style name="AlertDialogFastStyling2.Title" parent="RtlOverlay.DialogWindowTitle.AppCompat">
        <item name="android:textAppearance">@style/AlertDialogFastStyling.Title.TextAppearance</item>
    </style>

    <!-- Стиль для текста заголовка диалога (цвет и размер шрифта) -->
    <style name="AlertDialogFastStyling2.Title.TextAppearance">
        <item name="android:textSize">@dimen/abc_text_size_title_material</item>
        <item name="android:textColor">@color/abc_primary_text_material_light</item>
    </style>

    <!-- Стиль для кнопок в диалоге -->
    <style name="AlertDialogFastStyling2.Button" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#0C0D0D</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
